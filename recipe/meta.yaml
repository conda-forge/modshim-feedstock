{% set name = "modshim" %}
{% set version = "0.3.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/modshim-{{ version }}.tar.gz
  sha256: 6958ea03b32992542cde275c1767594cc8e219f6fdb6255d868f4abbba6eec69

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}

test:
  imports:
    - modshim
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/joouha/modshim
  dev_url: https://github.com/joouha/modshim
  summary: Override and customize Python packages without touching their code.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - apcamargo
